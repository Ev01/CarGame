#version 330 core

//out vec4 FragColor;

void main()
{
    // Empty as no processing needs to be done for colour (only depth)
    //gl_FragDepth = gl_FragCoord.z; // This happens automatically anyway
    //FragColor = vec4(gl_FragCoord.zzz, 1.0);
}
